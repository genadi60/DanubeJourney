@using DanubeJourney.Data
@inject DanubeJourneyDbContext DbContext;
@{
    ViewData["Title"] = "_ShipsPartial";
    var ships = DbContext.Ships;
}

@foreach (var ship in ships)
{
    <li>
        <a asp-controller="Ships" asp-action="Details" asp-route-id="@ship.Id">@ship.Name</a>
    </li>
}
